# SyncGUI - 双端文件同步工具

## 项目简介

SyncGUI 是一款简洁高效的双端双向文件同步工具。支持智能差异检测、多种同步模式、实时进度显示，提供流畅的用户体验。采用 PyQt5 架构，Windows API 高效文件复制，MD5 哈希内容比对。

## 项目信息

- **项目名称**: SyncGUI
- **项目版本**: R11
- **项目作者**: Lisselde_E
- **联系邮箱**: Lisselde.E@outlook.com
- **项目仓库**: https://github.com/LisseldeE/SyncGUI
- **国内构建版本下载镜像**: https://gitee.com/Lisselde_E/SyncGUI （推荐国内用户使用）

## 下载

- **GitHub Releases**: https://github.com/LisseldeE/SyncGUI/releases
- **Gitee 镜像下载**: https://gitee.com/Lisselde_E/SyncGUI/releases （推荐国内用户使用）

## 功能特性

### 核心功能

- **双向同步** - 支持双端之间的双向文件同步
- **智能差异检测** - 自动识别新增、修改、冲突文件
- **目录级别同步** - 可配置特定目录按整体同步，减少重复操作
- **空目录同步** - 正确处理空目录及其目录结构
- **符号链接支持** - 完整支持符号链接的识别与同步
- **文件属性同步** - 同步文件的只读、隐藏、系统等属性

### 同步模式

#### 双向同步
- **手动选择** - 对每个差异文件弹窗询问同步方向
- **最新优先** - 自动选择较新版本同步，减少弹窗询问

#### 单向同步
支持双端之间的单向同步，包含两种子模式：

**差异同步模式**：
- 源缺失项目：根据"保留/删除多余项目"按钮决定处理方式
- 目标缺失项目：自动补齐
- 差异项目：若源新于目标则覆盖，若目标新于源则忽略

**覆盖同步模式**：
- 源缺失项目：根据"保留/删除多余项目"按钮决定处理方式
- 目标缺失项目：自动补齐
- 差异项目：无视时间戳，始终源覆盖目标

**多余项目处理**：
- "保留多余项目"（绿色）：保留目标端多余文件
- "删除多余项目"（红色）：删除目标端多余文件

### 差异处理策略

| 状态 | 说明 |
|------|------|
| 一端独有 | 弹窗选择同步到另一端或删除 |
| 内容冲突 | 弹窗选择保留哪一端版本 |
| 时间差异 | 弹窗选择覆盖方向 |
| 目录同步 | 按配置规则整体同步 |

### 自定义名称

- 支持自定义双端目录名称显示
- 名称配置自动保存，下次启动自动恢复
- 点击名称标签可快速修改显示名称

### 多语言支持

- 支持中文/英文界面切换
- 实时切换语言无需重启程序
- 适配多语言环境

### 界面优化

- PyQt5 图形界面，界面简洁直观
- 弹窗统一样式，操作简洁直观
- 支持查看详细文件列表
- 实时进度显示
- 所有设置自动保存，下次启动自动恢复
- 按钮点击动画效果（按下时向下移动1px）
- 模式切换按钮淡入淡出过渡动画（200ms平滑过渡）
- 检查更新功能

### 配置管理

- 配置文件自动保存用户设置
- 配置路径位于 AppData，适配微软商店环境
- 支持忽略规则、同步规则配置
- 支持自定义忽略规则语法

## 使用方法

1. 运行 `SyncGUI.exe` 或 `python main.py`
2. 选择两个同步目录（可自定义名称）
3. 点击"扫描差异"检测文件差异
4. 根据弹窗提示选择同步方向（可点击"取消本次同步"终止）
5. 点击"执行同步"执行操作

## 配置说明

配置文件 `config.json` 自动保存用户设置，无需手动修改：

```json
{
  "A_dir": "A路径",
  "B_dir": "B路径",
  "ignore_rules": [
    "SyncGUI/",
    "**/.git/"
  ],
  "sync_rules": [
    "Python/",
    "Servers/"
  ],
  "language": "zh",
  "sync_mode": "newest",
  "removable_name": "A",
  "local_name": "B"
}
```

### 配置字段说明

| 字段 | 说明 |
|------|------|
| `A_dir` | A端目录路径 |
| `B_dir` | B端目录路径 |
| `ignore_rules` | 忽略规则列表 |
| `sync_rules` | 同步规则列表 |
| `language` | 界面语言 (`zh` 中文 / `en` 英文) |
| `sync_mode` | 同步模式 (`default` 手动选择 / `newest` 最新优先 / `unidirectional` 单向同步) |
| `removable_name` | A端自定义名称 |
| `local_name` | B端自定义名称 |

### 忽略规则语法

| 规则 | 匹配范围 |
|------|----------|
| `目录名/` | 仅匹配根目录下的指定目录 |
| `**/目录名/` | 匹配任意位置的指定目录 |
| `.扩展名` | 匹配所有指定扩展名的文件 |

### 同步规则

配置 `sync_rules` 后，指定目录下的第一级子目录将按整体同步，减少逐文件询问。

## 技术实现

- Python + PyQt5 图形界面
- Windows API 高效文件复制
- MD5 哈希内容比对
- 进度回调节流优化

## 系统要求

- Windows 10/11
- Python 3.8+ (源码运行)

## 更新日志

### R10 (2026.6.27)
**#01**
- 修改配置文件字段名：wintogo_dir → A_dir, local_dir → B_dir
- 配置文件存储路径迁移至 AppData\\Roaming\\SyncGUI
- 适配微软商店 MSIX 打包环境的沙箱权限要求
- 新增自定义双端名称功能
- 修改"默认模式"为"手动选择"
- 初次启动默认模式改为"最新优先"
- 忽略规则提示添加 `**/` 用法说明
- 优化弹窗界面样式，移除硬编码背景色和文字颜色
- 优化语言切换按钮尺寸，完整显示图标和文字
- 优化关于界面文字大小

**#02**
- 修复配置加载时名称尾缀缺失问题
- 修复修改名称后差异列表未刷新问题
- 修复差异列表状态文字硬编码问题
- 完善所有弹窗界面文字显示逻辑

### R10 (2026.6.6)
**#01**
- 新增！单向同步模式以及附属设置项
- 新增模式切换时的自刷新
- 优化界面按钮的显示逻辑
- 修复了对文件路径读取的逻辑
- 修复了同步文件时的弹窗逻辑与选项内容
- 完整规范了文件同步逻辑
- 完整重构了文件的删除逻辑与父级空目录的处理逻辑
- 添加了同步确认窗缺少的统计条目
- 新增了界面按钮元素的过渡动画与点击效果
- 新增了检查更新的功能

### R9 (2026.6.4)
**#01**
- 修复高DPI缩放问题（150%缩放界面过大）
- 最新优先模式下自动隐藏同步规则按钮
- 调整按钮布局顺序，模式切换按钮紧邻同步按钮
- 优化界面在高分辨率显示器上的显示效果

### R8 (2026.6.4)
**#01**
- 新增界面中文/英文切换功能
- 为所有界面元素适配英文模式
- 新增"最新优先"同步模式
- 修复按钮显示逻辑，优化细节
- 修复部分界面元素的更新逻辑
- 更新配置文件结构适配新功能

### R7 (2026.5.28)
**#01**
- 新增 `**/` 全局文件夹排除语法
- 修复同步时文件属性丢失问题

### R6 (2026.5.28)
**#01**
- 修复同步逻辑异常
- 修复忽略规则处理问题

### R5 (2026.5.28)
**#01**
- 修复空文件夹同步问题
- 统一弹窗界面样式

### R4 (2026.5.16)
**#01**
- 全面优化同步算法
- 修复大目录多次弹窗问题

### R3 (2026.5.15)
**#01**
- 修复配置路径问题

### R2 (2026.5.14)
**#01**
- 新增忽略项配置
- 新增详细进度条
- 更新软件名称

### R1 (2026.5.13)
**#01**
- 初次构建
- 实现基本同步功能

## 开源声明

本项目采用 MIT 开源协议。

## 联系与反馈

**开发中应用，如有问题或新的创意欢迎和我联系！**

- 📧 邮箱: Lisselde.E@outlook.com
- 🐙 GitHub: https://github.com/LisseldeE/SyncGUI

欢迎提交 Issue 和 Pull Request！