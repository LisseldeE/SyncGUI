- 忽略规则：
    - `目录名/` 仅匹配根目录下的指定目录
    - `**/目录名/` 匹配任意位置的指定目录
    - `.扩展名` 匹配所有指定扩展名的文件（如 `.log`）
    - `目录名/.扩展名` 匹配指定目录下的指定扩展名文件
- 同步规则：
    -配置后，指定目录下的第一级子目录将按整体同步，减少逐文件询问。
- 特殊文件处理
    - 符号链接：识别并记录符号链接及其目标路径
    - 文件属性同步：同步只读、隐藏、系统、存档等属性

- 双向同步
    - 最新优先（忽略规则生效、同步规则失效）
        - 补齐双端的缺失项目
        - 不弹窗询问，以最新版覆盖差异项目
        - 此模式下不删除内容，始终为补齐或覆盖
    - 默认模式（忽略规则生效、同步规则生效）
        - 缺失项目：弹窗询问（另一端补齐此项目、双端删除此项目、跳过）
        - 差异项目：弹窗询问（哪一端覆盖哪一端、双端删除此项目、跳过）
- 单向同步
    - 新增切换按钮“保留多余项目”（绿色）/“删除多余项目”（红色）（用于减少交互次数）
    - 差异同步（忽略规则生效、同步规则失效）
        - 源缺失项目
            - “保留多余项目”模式，则忽略目标多余项目
            - “删除多余项目”模式，则删除目标多余项目，执行后续逻辑
        - 目标缺失项目：补齐此项目
        - 差异项目：若源新于目标，则覆盖目标；若目标新于源，则忽略此项目
        - 列表显示逻辑
            - 源存在，目标不存在，显示为源独有（具体显示本地或介质），状态显示“同步至目标”
            - 源、目标都存在，若源新于目标，则显示为差异项，状态显示“覆盖目标”
            - 源、目标都存在，若目标新于源，则忽略此项目
            - 目标存在，源不存在
                - “保留多余项目”模式，则忽略多余项目
                - “删除多余项目”模式，显示为目标独有（具体显示本地或介质），列表标记为红色，状态显示“将被删除”
    - 覆盖同步（忽略规则生效、同步规则失效）
        - 源缺失项目
            - “保留多余项目”模式，则忽略多余项目
            - “删除多余项目”模式，则删除目标多余项目，执行后续逻辑
        - 目标缺失项目：补齐此项目
        - 差异项目：无视新旧，始终源覆盖目标
        - 列表显示逻辑
            - 源存在，目标不存在，显示为源独有（具体显示本地或介质），状态显示“同步至目标”
            - 源、目标都存在，忽略时间戳，始终为源同步至目标
            - 目标存在，源不存在
                - “保留多余项目”模式，则忽略目标多余项目
                - “删除多余项目”模式，显示为目标独有（具体显示本地或介质），列表标记为红色，状态显示“将被删除”

- 空目录（空文件夹）的处理逻辑
    - 扫描阶段：识别所有空文件夹（包括叶子空文件夹和其父空文件夹）
    - 比较阶段：
        - 如果文件夹有内容，则忽略文件夹本身，只处理其内部文件
        - 如果文件夹为空且另一端不存在，则参与同步逻辑
        - 如果一端有内容，另一端为空文件夹，则以有内容为主
    
- 删除逻辑
    - 删除操作按路径深度排序执行（父目录优先）
    - 如果父目录已被删除，自动跳过其子内容的删除操作
    - Windows系统：删除前自动移除只读/系统/隐藏属性
    - 使用安全删除机制，确保删除操作的可靠性